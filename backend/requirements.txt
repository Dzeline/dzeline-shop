fastapi>=0.115.0
uvicorn[standard]>=0.30.6
# Capped to the line this code is written and tested against. The service
# rebuilds rarely - months apart - so an open upper bound means a rebuild can
# quietly resolve to a version nothing here has ever run on.
sqlalchemy>=2.0.35,<2.1
pydantic>=2.9.2
python-dotenv>=1.0.1
httpx>=0.27.2
psycopg2-binary>=2.9.9
slowapi>=0.1.9
