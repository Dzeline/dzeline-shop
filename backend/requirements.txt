fastapi>=0.115.0
uvicorn[standard]>=0.30.6
# Capped: this code is written against the 2.0 line. 2.1 changes the default
# postgresql driver to psycopg v3, which is not what psycopg2-binary below
# provides - an unpinned rebuild crashed the service on boot because of it.
sqlalchemy>=2.0.35,<2.1
pydantic>=2.9.2
python-dotenv>=1.0.1
httpx>=0.27.2
psycopg2-binary>=2.9.9
slowapi>=0.1.9
Pillow>=10.0.0
opencv-python-headless>=4.8.0
paddlepaddle>=2.6.0
paddleocr>=2.7.0
